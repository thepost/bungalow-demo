const white = "#FFFFFF"
const black = "#000000"
const lightGrey = "#A3A3A3"
const darkGrey = "#5C5C5C"
const primaryTheme = "#A9E3D0"
const secondaryTheme = "#EF835D"

const border = lightGrey

const colors = {
  white,
  black,
  lightGrey,
  darkGrey,
  primaryTheme,
  secondaryTheme,
  border
}

const fontSizes = {
  title1: 24,
  title2: 20,
  body: 16,
  detail: 12
}

const fonts = {
  primary: "Helvetica Neue"
}

const alignment = {
  center: "center",
  left: "left",
  right: "right",
  flexStart: "flex-start",
  flexEnd: "flex-end"
}

export { colors, fontSizes, fonts, alignment }
